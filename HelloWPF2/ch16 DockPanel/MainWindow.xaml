<Window x:Class="ch16_DockPanel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ch16_DockPanel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel LastChildFill="True">
        <!-- 1. 상단 메뉴 -->
        <Menu x:Name="Menu" DockPanel.Dock="Top">
            <MenuItem Header="파일">
                <MenuItem Header="새로 만들기"/>
                <MenuItem Header="열기"/>
                <MenuItem Header="저장"/>
                <Separator/>
                <MenuItem Header="종료"/>
            </MenuItem>
            <MenuItem Header="편집">
                <MenuItem Header="잘라내기"/>
                <MenuItem Header="복사"/>
                <MenuItem Header="붙여넣기"/>
            </MenuItem>
            <MenuItem Header="도움말">
                <MenuItem Header="정보"/>
            </MenuItem>
        </Menu>
        <!-- 2. 하단 상태바 -->
        <StatusBar x:Name="StatusBar" DockPanel.Dock="Bottom">
            <StatusBarItem Content="준비"/>
        </StatusBar>
        <!-- 3. 중앙 영역 (Grid + GridSplitter) -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" MinWidth="100"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" MinWidth="100"/>
            </Grid.ColumnDefinitions>

            <!-- 좌측 트리뷰 -->
            <TreeView x:Name="TreeView" Grid.Column="0">
                <TreeViewItem Header="폴더 1" IsExpanded="True">
                    <TreeViewItem Header="파일 1"/>
                    <TreeViewItem Header="파일 2"/>
                </TreeViewItem>
                <TreeViewItem Header="폴더 2">
                    <TreeViewItem Header="파일 3"/>
                </TreeViewItem>
            </TreeView>

            <GridSplitter Grid.Column="1" Width="5"
                          HorizontalAlignment="Center" VerticalAlignment="Stretch"
                          Background="LightGray" ShowsPreview="True"/>

            <TextBox x:Name="MainContent" Grid.Column="2"
                     AcceptsReturn="True" AcceptsTab="True"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
        </Grid>
    </DockPanel>
</Window>
