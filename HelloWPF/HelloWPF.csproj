<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="ch21 스테이터스바\**" />
    <Compile Remove="ch35 MariaDB연동\**" />
  </ItemGroup>

  <!-- 현황판은 기록 모델만 공유합니다. 후킹 로직(StudyTracker)은 챕터 앱 전용입니다. -->
  <ItemGroup>
    <Compile Include="..\Shared\StudyRecords.cs" Link="Shared\StudyRecords.cs" />
  </ItemGroup>

  <ItemGroup>
    <EmbeddedResource Remove="ch21 스테이터스바\**" />
    <EmbeddedResource Remove="ch35 MariaDB연동\**" />
  </ItemGroup>

  <ItemGroup>
    <Page Remove="ch21 스테이터스바\**" />
    <Page Remove="ch35 MariaDB연동\**" />
  </ItemGroup>

  <ItemGroup>
    <None Remove="ch21 스테이터스바\**" />
    <None Remove="ch35 MariaDB연동\**" />
  </ItemGroup>

</Project>
