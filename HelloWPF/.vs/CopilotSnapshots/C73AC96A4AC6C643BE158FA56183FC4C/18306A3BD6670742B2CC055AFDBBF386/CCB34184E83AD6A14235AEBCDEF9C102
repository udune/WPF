<!-- 
    ch6 패스워드박스 - WPF PasswordBox 컨트롤을 보여주는 예제 파일
    PasswordBox는 비밀번호와 같은 민감한 정보를 안전하게 입력받기 위한 컨트롤입니다.
    보안상의 이유로 TextBox와 달리 입력된 문자를 마스킹하여 표시합니다.
-->
<Window x:Class="ch6_패스워드박스.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ch6_패스워드박스"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <!-- StackPanel: 자식 요소들을 세로 방향으로 순차적으로 배치 -->
        <StackPanel>
            <!-- 
                PasswordBox: 비밀번호 입력을 위한 특수 컨트롤
                
                주요 특징:
                - 입력된 문자를 PasswordChar로 지정한 문자로 마스킹하여 표시
                - 복사/붙여넣기가 기본적으로 비활성화되어 있어 보안성이 높음
                - TextBox와 달리 Text 속성이 없고 Password 속성을 사용
                - 보안상의 이유로 Password 속성은 데이터 바인딩을 지원하지 않음
            -->
            <PasswordBox BorderThickness="3"
                         PasswordChar="*"
                         MaxLength="3"
                         x:Name="pwd"/>
            <!-- 
                PasswordBox 속성 상세 설명:
                
                BorderThickness="3": 테두리 두께를 3픽셀로 설정
                                     시각적으로 입력 영역을 강조
                
                PasswordChar="*": 입력된 문자를 "*" 문자로 마스킹하여 표시
                                  - 기본값은 "●" (검은 원)
                                  - 다른 문자로 변경 가능 (예: "#", "○" 등)
                                  - 실제 입력된 문자는 메모리에 저장되고 화면에는 마스킹 문자만 표시
                
                MaxLength="3": 최대 입력 가능한 문자 수를 3자로 제한
                               - 사용자가 3자를 초과하여 입력할 수 없음
                               - PIN 번호나 짧은 비밀번호 입력에 유용
                
                x:Name="pwd": 코드 비하인드에서 접근할 수 있는 고유 식별자
                              이름을 통해 C# 코드에서 Password 속성에 접근 가능
                
                참고 - PasswordBox vs TextBox:
                PasswordBox:
                - Password 속성 사용 (string 타입)
                - 보안을 위해 입력 문자를 마스킹
                - 복사/붙여넣기 비활성화 (보안)
                - 데이터 바인딩 미지원 (보안상 이유)
                - Undo/Redo 기능 없음
                
                TextBox:
                - Text 속성 사용
                - 입력 문자를 그대로 표시
                - 복사/붙여넣기 지원
                - 데이터 바인딩 지원
                - Undo/Redo 기능 지원
            -->
            
            <!-- 
                Button: PasswordBox에 입력된 비밀번호를 확인하는 버튼
                Click="Button_Click": 버튼 클릭 시 실행될 이벤트 핸들러
                                      MessageBox로 입력된 비밀번호를 표시
            -->
            <Button Click="Button_Click">패스워드확인</Button>
        </StackPanel>
    </Grid>
    
    <!--
        PasswordBox 추가 유용한 속성:
        
        - SelectionBrush: 선택된 텍스트의 배경색 설정
        - SelectionOpacity: 선택 영역의 투명도 설정
        - CaretBrush: 커서(캐럿)의 색상 설정
        - PasswordRevealMode: (UWP) 비밀번호 표시/숨김 토글 기능
        
        보안 고려사항:
        
        1. SecureString 사용:
           - PasswordBox.SecurePassword 속성으로 SecureString 객체 반환
           - 메모리에 암호화되어 저장되므로 더 안전
           - 가비지 컬렉션 시 자동으로 메모리에서 제거
        
        2. Password 속성 사용 주의:
           - Password 속성은 string 타입으로 평문 반환
           - 메모리에 평문으로 저장되어 보안에 취약할 수 있음
           - 가능한 SecurePassword를 사용하는 것이 권장됨
        
        3. MVVM 패턴에서의 사용:
           - 데이터 바인딩이 지원되지 않으므로 Attached Property나
             Behavior를 사용하여 구현해야 함
        
        예제 - SecureString 사용:
        SecureString securePassword = pwd.SecurePassword;
        
        예제 - 비밀번호 유효성 검사:
        if (pwd.Password.Length < 8)
        {
            MessageBox.Show("비밀번호는 8자 이상이어야 합니다.");
        }
    -->
</Window>
