<!-- 
    ch4 버튼 - WPF Button 컨트롤의 다양한 기능과 이벤트를 보여주는 예제 파일
    Button은 사용자와의 상호작용을 위한 가장 기본적인 컨트롤입니다.
-->
<Window x:Class="ch4_버튼.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ch4_버튼"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <!-- StackPanel: 자식 요소들을 세로 방향으로 순차적으로 배치 -->
        <StackPanel>
            <!-- 
                예제 1: Button 기본 사용법 - 태그 사이에 텍스트 직접 작성
                간단한 버튼 정의 방식
            -->
            <Button>버튼</Button>
            
            <!-- 
                예제 2: Button 기본 사용법 - Content 속성 사용
                더 명시적이고 권장되는 방식
            -->
            <Button Content="버튼"/>
            
            <!-- 
                예제 3: Button의 다양한 속성과 이벤트
                실제 프로젝트에서 사용되는 버튼의 완전한 형태
            -->
            <Button
                x:Name="btn"
                ToolTip="클릭해보세요."
                Click="btn_Click"
                MouseDoubleClick="btn_MouseDoubleClick"
                MouseEnter="btn_MouseEnter"
                MouseLeave="btn_MouseLeave"
                Content="버튼" Width="100">
                <!-- 
                    Button 속성 설명:
                    - x:Name="btn": 코드 비하인드에서 접근할 수 있는 고유 식별자
                    - ToolTip: 마우스를 버튼 위에 올렸을 때 표시되는 도움말 텍스트
                    - Click: 버튼을 클릭했을 때 실행되는 이벤트 핸들러
                    - MouseDoubleClick: 버튼을 더블 클릭했을 때 실행되는 이벤트 핸들러
                    - MouseEnter: 마우스가 버튼 영역에 들어왔을 때 실행되는 이벤트 핸들러
                    - MouseLeave: 마우스가 버튼 영역에서 벗어났을 때 실행되는 이벤트 핸들러
                    - Content: 버튼에 표시될 텍스트
                    - Width: 버튼의 너비를 100픽셀로 고정
                -->
                
                <!-- 
                    Button.Resources: 버튼의 로컬 리소스 정의 영역
                    이 버튼에만 적용되는 스타일을 정의합니다.
                -->
                <Button.Resources>
                    <!-- 
                        Style: Border 요소에 적용될 스타일 정의
                        TargetType="Border": 이 스타일이 Border 타입에 적용됨을 지정
                    -->
                    <Style TargetType="Border">
                        <!-- 
                            Setter: 속성 값을 설정
                            Property="CornerRadius": 모서리 둥글기 속성
                            Value="10": 모서리를 10픽셀 반경으로 둥글게 만듦
                            
                            참고: Button 내부의 Border 요소에 적용되어 버튼의 모서리가 둥글게 표시됩니다.
                        -->
                        <Setter Property="CornerRadius" Value="10"></Setter>
                    </Style>
                </Button.Resources>
            </Button>
            
            <!-- 
                TextBlock: 버튼 이벤트의 결과를 표시하기 위한 텍스트 블록
                x:Name="txtb": 코드 비하인드에서 이 TextBlock의 Text 속성을 변경하여
                             버튼 이벤트 발생 시 메시지를 표시할 수 있습니다.
            -->
            <TextBlock Text="텍스트블럭" x:Name="txtb"/>
        </StackPanel>
    </Grid>
</Window>
