<Window x:Class="ch30_탭컨트롤_모달_모달리스.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ch30_탭컨트롤_모달_모달리스"
        mc:Ignorable="d"
        Title="대화상자" Height="250" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 헤더 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="모달/모달리스 테스트 창" FontSize="18" FontWeight="Bold" Foreground="#333"/>
            <TextBlock Text="이 창을 열었을 때 부모 창을 클릭해보세요." Foreground="#666" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- 내용 -->
        <Border Grid.Row="1" Background="#F5F5F5" CornerRadius="5" Padding="15">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="모달 창:" FontWeight="Bold"/>
                <TextBlock Text="• 이 창을 닫기 전까지 부모 창을 클릭할 수 없습니다." TextWrapping="Wrap"/>
                <TextBlock Text="" Margin="0,10,0,0"/>
                <TextBlock Text="모달리스 창:" FontWeight="Bold"/>
                <TextBlock Text="• 이 창이 열려있어도 부모 창을 자유롭게 사용할 수 있습니다." TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- 버튼 영역 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="확인" Click="OK_Click" Width="80" Height="32" Margin="0,0,10,0"
                    Background="#4CAF50" Foreground="White" BorderThickness="0"
                    IsDefault="True"/>
            <Button Content="취소" Click="Cancel_Click" Width="80" Height="32"
                    Background="#9E9E9E" Foreground="White" BorderThickness="0"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
